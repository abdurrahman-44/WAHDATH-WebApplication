{
  "name": "wahdath-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "WAHDATH AI Quran assistant backend — retrieval + a scoped, source-grounded Claude call. Never runs in the browser (section 42: API keys stay server-side).",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "web-push": "^3.6.7"
  }
}
